{
  "name": "@cf-realtimekit-demo/worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "pnpm --dir ../.. build:web && wrangler dev --config ../../wrangler.jsonc --local",
    "dev:worker": "pnpm dev",
    "test": "tsx --test src/app.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "hono": "^4.10.7"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "tsx": "^4.21.0"
  }
}
